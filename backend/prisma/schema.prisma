generator client {
    provider = "prisma-client-js"
    moduleFormat = "esm"
}

datasource db {
    provider = "postgresql"
    url = env("DB_URL")
}

model Task {
    id          Int      @id @default(autoincrement())
    title       String
    description String?
    column      Column   @relation(fields: [columnId], references: [id])
    columnId    Int
    orderId     Float
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt

    @@unique([orderId, columnId])
}

model Column {
    id        Int      @id @default(autoincrement())
    name      String   @unique
    tasks     Task[]
    color     String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}